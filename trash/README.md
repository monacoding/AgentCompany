# trash/

Cursor/AI 세션에서 실수로 생성된 루트 잔여 파일 보관용입니다.  
**프로젝트 빌드·실행과 무관**하며 git에는 포함하지 않습니다.

## cursor-artifacts-2026-06-11

| 파일 | 내용 요약 |
|------|-----------|
| `bash`, `shell` | 마크다운 코드펜스 언어 태그(`bash`)만 저장된 1줄 (`npm test -- --coverage`) |
| `json` | 에이전트 작업 요약 JSON 조각 |
| `python` | PyPDF2 수능 PDF 분할 일회성 스크립트 (placeholder 경로) |
| `typescript` | 미사용 ImageApiService 스니펫 |
| `plaintext:server.js` | Express hello-world 샘플 (파일명에 `plaintext:` 접두 포함) |

원본은 2026-06-11 루트에서 제거되었습니다.
